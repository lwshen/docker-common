name: Lint Dockerfiles

on: [push, pull_request]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2.4.0
      - run: brew install hadolint
      - run: |
          hadolint --ignore DL3008 jdk/{8,11,17}/*Dockerfile
          hadolint jre8/*Dockerfile
